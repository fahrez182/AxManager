<resources>
    <!-- Base application theme. -->
    <style name="Theme.AxManager" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange</item>
    </style>

    <style name="Theme.AxManager.WebUI" parent="Theme.AxManager">
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

</resources>